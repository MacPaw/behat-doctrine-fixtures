behat_doctrine_fixtures:
  connections:
    default:
      runMigrationsCommand: 'bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration'
      databaseFixturesPaths:
        - '%kernel.project_dir%/Fixtures/ORM'
